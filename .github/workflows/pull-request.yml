name: Pull Requests

on:
  pull_request:
    paths-ignore:
      - '.github/**'

jobs:
  Build:
    uses: tylerbertrand/morphia-workflows/.github/workflows/pull-request.yml@master
    with:
      java: 17
